#include <stdio.h>
void main()
{
    int a;
    printf("Enter a number:");
    scanf("%d",&a);
    if (a>0)
    printf("%d  is positive number ",a);
    else if(a==0)
    printf("Enter number is Zero");
    else
    printf("%d  is negitive number",a);
    
}
